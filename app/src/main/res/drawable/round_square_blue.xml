<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_pressed="true">
        <!-- 定义当button 处于pressed 状态时的形态。 -->
        <shape>
            <gradient android:angle="90" android:endColor="@color/blue" android:startColor="@color/red" android:type="linear"/>
            <corners android:radius="5dp"/>
            <stroke android:width="2dp" android:color="@color/red"/>
            <padding android:bottom="5dp" android:left="5dp" android:right="5dp" android:top="5dp"/>
        </shape>
    </item>
    <item android:state_pressed="false">
        <!-- 定义当button 处于pressed 状态时的形态。 -->
        <shape>
            <gradient android:angle="90" android:endColor="@color/blue" android:startColor="@color/blue" android:type="linear"/>
            <corners android:radius="5dp"/>
            <stroke android:width="2dp" android:color="@color/blue"/>
            <padding android:bottom="5dp" android:left="5dp" android:right="5dp" android:top="5dp"/>
        </shape>
    </item>
    <item android:state_focused="true">
        <!-- 定义当button获得 focus时的形态 -->
        <shape>
            <!-- 设置渐变 -->
            <gradient android:angle="90" android:endColor="@color/red" android:startColor="@color/red" android:type="linear"/>
            <!-- 设置圆角 -->
            <corners android:radius="5dp"/>
            <!-- 设置边缘线 -->
            <stroke android:width="2dp" android:color="@color/red"/>
            <!-- 设置偏移量 -->
            <padding android:bottom="5dp" android:left="5dp" android:right="5dp" android:top="5dp"/>
        </shape>
    </item>

</selector>